name: Flujo en Windows

on:
  push:
    branches:
      - main

jobs:
  ejecutar-en-windows:
    runs-on: windows-latest
    steps:
      - name: Dice Hola
        run: echo "¡Hola, Comprobando...!"
